# =============================================================================
# Anomaly Detection - Environment Configuration
# =============================================================================
# Copy this file to .env and modify as needed
# For RunPod: Set these in the pod's environment variables

# -----------------------------------------------------------------------------
# Mode Selection
# -----------------------------------------------------------------------------
# false = API server (Pod mode), true = Serverless handler
RUNPOD_SERVERLESS=false

# -----------------------------------------------------------------------------
# Detector Configuration (RF-DETR)
# -----------------------------------------------------------------------------
# Model variant: nano, small, medium, large
# - nano:   48.4 mAP, 2.32ms (fastest)
# - small:  53.0 mAP, 3.52ms
# - medium: 54.7 mAP, 4.52ms (recommended)
# - large:  56.7 mAP, 9.64ms (most accurate)
DETECTOR_VARIANT=medium

# Path to custom fine-tuned weights (optional, leave empty for COCO pretrained)
DETECTOR_WEIGHTS=

# Number of classes if using custom weights (optional)
DETECTOR_NUM_CLASSES=

# Detection confidence threshold (0.0 - 1.0)
CONFIDENCE_THRESHOLD=0.3

# -----------------------------------------------------------------------------
# Segmenter Configuration (SAM3)
# -----------------------------------------------------------------------------
# Path to SAM3 model weights
SAM_MODEL_PATH=/models/sam3.pt

# -----------------------------------------------------------------------------
# API Server Configuration (Pod mode only)
# -----------------------------------------------------------------------------
API_HOST=0.0.0.0
API_PORT=8000

# -----------------------------------------------------------------------------
# Optional Features
# -----------------------------------------------------------------------------
# Save segmentation masks to disk
SAVE_MASKS=true

# Directory for saved masks (required if SAVE_MASKS=true)
MASK_OUTPUT_DIR=/tmp/masks
